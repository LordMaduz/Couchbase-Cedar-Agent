server:
  port: 8070
  http2:
    enabled: true

spring:
  application:
    name: cedar-agent
  main:
    web-application-type: reactive
  webflux:
    base-path: /api/cedar-agent
  data:
    mongodb:
      host: localhost
      port: 27017
      database: cedar-authorization-service

#logging:
#  level:
#    org.springframework.data.couchbase.core: TRACE